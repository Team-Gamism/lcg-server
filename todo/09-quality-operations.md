# 검증·배포·운영

아래는 앞으로 서버를 구현할 때의 검증 작업이다. 현재 저장소에는 실행할 서버나 테스트 프로젝트가 없으므로 통과했다고 표시하지 않는다. 각 기능 구현 시 위험이 있는 동작을 검증하고, 이 단계에서는 실제 사용 흐름과 운영 준비를 확인한다.

## 검증 TODO

- [ ] **QA-01 | P1 | 인증·개인정보·리소스 권한 통합 검증** — 선행: `AUTH-01~05`, `USER-01~04`, `COM-01~04`.
  - 구현: DataGSM 모의 HTTP 서버, 실제 PostgreSQL/Redis 통합 환경, 인증 없는/일반/타인/정지/운영자 접근 테스트를 준비한다. 정상 가입·재로그인·학교 정보 변경·PKCE/state 위조·동시 콜백·CSRF·로그아웃을 확인한다.
  - 완료 기준: 실패 경로에서도 회원 중복·세션 오발급·원본 개인정보 노출이 없다. 실제 개발 클라이언트가 준비되면 브라우저 리다이렉트와 쿠키를 포함한 로그인 smoke test도 수행한다.

- [ ] **QA-02 | P1 | MVP 사용자 흐름과 Riot 실패 검증** — 선행: `QA-01`, `USER-03`, `MAT-01~03`, `RANK-01~02`, `HOME-01`.
  - 구현: 학교 로그인 → Riot 등록/검증 상태 → 검색/전적 → 학교 순위 → 글·댓글·추천 → 공개 설정 변경 → 로그아웃을 검증한다. Riot 404/429/5xx/타임아웃, 캐시 미스·만료·동시 갱신, 페이지 경계·동률·시즌 변경을 확인한다.
  - 완료 기준: 미인증 계정의 학교 랭킹 처리 정책이 UI/API에서 일치하고 `D-02` 미해결을 통과로 처리하지 않는다. 외부 API가 실패해도 기존 데이터/명확한 상태를 제공한다. CI는 실제 API 키 없이 결정적으로 실행된다.

- [ ] **QA-03 | P2 | 모집 경쟁·실시간·작업 복구 검증** — 선행: `PTY-01~05`, `RT-01~02`, `INH-01~06`, `NTF-01~02`.
  - 구현: 마지막 자리 동시 신청·승인, 중복 슬롯·방장 이탈·강퇴, 팀 재편성 충돌, reconnect·이벤트 역순·중복, 커밋 후 프로세스 종료·Outbox 재처리, 일정 취소와 알림 발송 경쟁을 검증한다.
  - 완료 기준: 정원·팀·권한 불변식이 유지되고 DB와 재접속 화면이 같은 상태다. 알림 유실/중복 정책이 입증된다. Redis 재시작이 영구 참가 데이터를 잃게 하지 않는다.

- [ ] **QA-04 | P3 | 통계·시간·동의·차단 검증** — 선행: `STAT-01~04`, `SOC-01~02`, `NTF-03`, `COM-05~06`.
  - 구현: 한 경기에 여러 학교 사용자, 중복 수집, 표본 0/29/30, 패치/큐/주 경계, 지연 경기, 탈퇴·동의 철회·차단·삭제 콘텐츠를 고정 데이터로 검증한다.
  - 완료 기준: 기대 분모/분자·게임 수와 플레이 건수가 일치한다. 재집계와 재실행 결과가 같고 공개 제한이 통계·활동·홈·알림에도 반영된다.

## 운영 TODO

- [ ] **OPS-01 | P0 | CI·설정·최소 관측** — 선행: `FND-02~05`.
  - 구현: 빌드·검증·핵심 테스트 CI, 환경별 비밀 주입, 구조화 로그·traceId·건강 상태, DB/Redis 연결 상태를 제공한다. 외부 API 키·학교 토큰·쿠키·callback code·userinfo 본문을 마스킹한다.
  - 완료 기준: 신규 변경의 빌드 실패를 탐지하고 비밀값 없이 CI를 재현한다. liveness는 프로세스 상태, readiness는 필수 저장소 준비 상태를 구분하며 운영 상태 경로는 민감한 내부 정보를 노출하지 않는다.

- [ ] **OPS-02 | P0·출시 전 | 배포·승인·데이터 운영 준비** — 선행: `QA-01~02`, `RIOT-01`, `OPS-01`.
  - 구현: 컨테이너/호스팅 환경, HTTPS·프록시·쿠키 도메인, CORS, DB 마이그레이션 절차, rollback, 학교 OAuth 운영 callback, Riot 키 승인·제품 등록, 개인정보 처리·탈퇴·보존 안내, 필요한 Riot 고지 적용 여부를 확인한다.
  - 완료 기준: 운영과 같은 설정으로 배포/재시작/rollback을 리허설한다. 실제 공개 범위에 맞는 Riot 키를 사용하며 승인되지 않은 RSO·자동 내전 수집은 비활성 상태다. 문서상 필수 고지 내용은 프론트와 전달/확인한다.

- [ ] **OPS-03 | P1·출시 전 | 백업·복원·호출 예산·장애 대응** — 선행: `OPS-01`, `RIOT-02~04`, `FND-03~04`.
  - 구현: PostgreSQL 자동 백업·실제 복원, 로그 보존, 세션 만료/재로그인 절차, API 오류율·지연·캐시 적중률·Riot 429·동기화 지연 지표와 운영 경보를 구성한다. 목표 동시 사용자 수와 API 지연·호출 예산을 정한 뒤 해당 규모로 측정한다.
  - 완료 기준: 백업 파일 존재 확인에 그치지 않고 임시 DB 복원으로 데이터 무결성을 확인한다. 학교/Riot/Redis 장애 대응과 키 교체 절차가 재현되고, 목표 부하에서 제한 초과·무제한 작업 적체가 없다.

## 각 기능 완료 시 공통 점검

- 체크 항목의 완료 기준과 OpenAPI 계약이 실제 구현에 맞는가?
- 새 테이블·인덱스·제약은 마이그레이션으로 재현되는가?
- 작성자/방장/참가자/운영자의 권한을 서버가 검사하는가?
- 공개 범위 변경·탈퇴가 캐시·파생 데이터까지 반영되는가?
- 외부 호출과 작업 재시도가 제한되며 부분 실패를 구분하는가?
- 해당 변경에 의미 있는 단위/통합/수동 검증 결과가 기록되어 있는가?

기능이 없는 상태에서 대규모 성능 수치나 운영 보장을 먼저 단정하지 않는다. 성능 목표와 보존 기간은 `FND-01`, `D-09`, 실제 배포 환경에 맞춰 확정한다.
