spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:15432/lcg}
    username: ${DATABASE_USERNAME:lcg}
    password: ${DATABASE_PASSWORD:lcg-local-only}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:16379}

server:
  address: 127.0.0.1
  servlet:
    session:
      cookie:
        secure: false

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

lcg:
  environment: local
  datagsm:
    callback-uri: ${DATAGSM_CALLBACK_URI:http://localhost:8080/api/v1/auth/school/callback}
    success-uri: ${LOGIN_SUCCESS_URI:http://localhost:3000}
  security:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
    public-docs: true
